sudo docker image prune -a


sudo docker-compose up -d
VOLUME: test_docker_mysql_dat
sudo docker exec -it mongo_db mongo --username "root" --password 


phpmyadmin: http://localhost:8080/
mongo-express : http://localhost:8081/
login-keyrock: http://localhost:3000/
login to app : http://localhost:4000/login.php

sudo docker-compose up -d --build
sudo docker-compose down


mongo
db.cinema_collection.insert({username: "thanosdel", user_role: "ADMIN"} )
db.cinema_collection.find({username: "thanosdel"})


keyrock credentials
admin@test.com
1234


sudo docker compose up -d --build
sudo docker exec -it mysql_db mysql -u root -p 
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' IDENTIFIED BY 'root_pass';
FLUSH PRIVILEGES;
exit;
sudo docker compose down
sudo docker-compose up -d --build
login to phpmyadmin and import the idm.sql from backups folder


sudo docker exec -it apache curl  -d '{  "name": "tdel@test.com", "password": "1234"}'    -H 'Content-Type: application/json' http://172.18.1.5:3000/v1/auth/tokens
